package main 		// everythin in Go must be inside package

import "fmt"		// so to use print func we need to Import package containing it 

func main{
	fmt.Print("Hello World")   // print only , cursor -> same line as output
	fmt.Println("Hello World") // print + add newline 
}